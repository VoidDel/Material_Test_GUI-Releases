# Material Test GUI Releases

Public release repository for Material Test GUI Windows installer packages.

The source code repository is private. This repository is used only for public GitHub Releases consumed by the application's automatic update checker.

Release assets include the Windows x64 installer and its SHA-256 checksum file.